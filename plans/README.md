# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Align the local and CI verification gates | P1 | S | - | DONE |
| 002 | Return `400 invalid_query` for reversed date ranges | P1 | S | 001 | DONE |
| 003 | Resolve dependency audit advisories | P1 | S/M | 001 | DONE |
| 004 | Batch performance run details behind one API call | P2 | M | 001 | TODO |
| 005 | Harden workout identity against source collisions | P2 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 001 comes first because later plans should run the same local gate that CI enforces.
- 002, 003, 004, and 005 can run independently after 001.
- 005 should not be mixed with unrelated API route changes because workout ids are route-visible after rebuild.

## Verification baseline

At audit time on commit `1040362`:

- `bun run check:ci` passed.
- `./node_modules/.bin/tsc -b --pretty false --noEmit` passed.
- `bun test` passed: 337 tests.
- `bun audit` failed with Hono and PostCSS advisories; plan 003 addresses this.

## Findings considered and rejected

- Bind host exposure for `Bun.serve`: rejected. A local check with installed Bun 1.3.13 reported the default host as `localhost`, so the hypothesis that the API binds to all interfaces by default was not supported in this repo.
- Large local health artifacts: rejected as a plan. `vitals.duckdb` is ignored and untracked, and the previously listed `export.xml` was not present by the time plans were written. No source-code remediation is needed.
- Missing indexes on all time-series tables: rejected for now. The repo uses DuckDB analytics over local data and has no measured slow query evidence in this audit; batch API work in plan 004 is a higher-confidence performance improvement.
- Stale API docs: folded into plan 004 only for the new batched route. A standalone docs-only plan is lower leverage than the selected five-plan set.
