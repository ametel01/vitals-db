# Implementation Plans

Generated by the improve skill on 2026-06-14 and updated on 2026-06-23. Execute in the order below unless dependencies say otherwise. Each executor must read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Normalize offset datetime filters before DuckDB queries | P1 | M | - | TODO |
| 002 | Bound public list limits | P1 | S | 001 | TODO |
| 003 | Hide internal 500 error messages from API clients | P1 | S | - | TODO |
| 004 | Batch performance-run context lookups | P2 | M | 002 | TODO |
| 005 | Extract dashboard page models and add characterization tests | P2 | L | - | TODO |
| 006 | Align route documentation with the implemented API | P3 | S | 002, 003 | TODO |
| 007 | Honor CLI help flags before strict argument parsing | P2 | S | - | TODO |
| 008 | Preflight rebuild input before clearing analytics data | P1 | M | 007 | TODO |
| 009 | Align range validation with public date-bound semantics | P1 | S | 001, 002 | TODO |
| 010 | Bind health serve to localhost by default | P1 | S | - | TODO |
| 011 | Make React Doctor pre-commit failures blocking | P2 | S | - | TODO |
| 012 | Label ground contact time correctly in performance mechanics | P2 | S | - | TODO |
| 013 | Derive Zone 2 status from the measured ratio | P2 | S | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 002 should follow 001 so all public range filters keep correct datetime semantics before public list behavior is tightened.
- 004 should follow 002 because bounding `/workouts/performance-runs` reduces the endpoint's worst-case blast radius before batching internals.
- 006 should follow 002 and 003 if those plans are executed, because it documents their public limit and error-response behavior.
- 008 should follow 007 because it adds behavior tests to the CLI test harness introduced there.
- 009 should follow 001 and 002 so range validation matches the normalized date-bound semantics and merges cleanly with list-limit schema edits.
- 010 is independent of the previous API contract fixes, but if 006 is still pending, include the new `HOST`/loopback behavior when reconciling docs.
- 012 and 013 both touch `apps/web/app/performance/page.tsx`. They are small and independent, but if 005 lands first, execute their intent in the extracted performance model/helper files instead of reintroducing page-local helpers.

## Verification baseline

Advisor ran these read-only checks during planning:

- `bun run check:ci` -> passed during prior planning and again on 2026-06-23
- `bun audit` -> no vulnerabilities found on 2026-06-23
- `bun outdated` -> showed non-blocking upgrade candidates: Biome 2.x, TypeScript 6.x, lefthook 2.x, `@types/bun` 1.3.x, react-doctor 0.5.4
- `bun test` -> 357 pass, 0 fail on 2026-06-23

Advisor intentionally did not run `bun run typecheck` or `bun run build:web` during the read-only audit because this repo's TypeScript project references and Next build write ignored artifacts. Executors changing code should run them as requested by each plan.

## Findings considered and rejected

- Hardcoded secret scan: no source secrets were found by targeted patterns; no plan needed.
- Committed generated build output: `dist`, `.next`, `*.duckdb`, and `export.*` are ignored and not tracked; local files are not a repo finding.
- CLI crop output path behavior: `health crop` writes to an explicit or same-directory output path as documented; no concrete overwrite or traversal bug was found.
- Dependency migration: `bun audit` is clean. Outdated dev tools are worth routine maintenance but lower leverage than the planned findings.
- Daily-comparison datetime handling: `getMetricWindowComparisons` normalizes `to` to a UTC date before building trailing windows; no separate plan needed.
- Crop parser line-splitting behavior: possible malformed/non-Apple XML edge cases exist, but committed fixtures and generated crop output use the expected Apple export shape; lower confidence than the planned rebuild preflight.
- Browser-only visual audit: not run during the standard improve pass; UI plans 012 and 013 are based on code-level evidence and should still be visually checked by their executors if they start a web build.
